# kubernetes
spring.application.name=trade-account-trigger-pfac-source
spring.cloud.kubernetes.discovery.enabled=true
spring.cloud.kubernetes.discovery.all-namespaces=true
  # Kubernetes ConfigMap
spring.cloud.kubernetes.config.enabled=true
spring.cloud.kubernetes.config.name=trade-account-trigger-tasks
spring.cloud.kubernetes.config.namespace=${SERVICE_NAMESPACE:default}
  # Kubernetes Secret - not currently enabled.
spring.cloud.kubernetes.secrets.name=velocity-db-secret
spring.cloud.kubernetes.secrets.namespace=default
